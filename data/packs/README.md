# Packs

Culture pack drafts, including output from `scripts/ggb_pack_draft.py`.

- Copy the pack out of the gitignored `out/` directory to land it here
- Keep the `.json` and `.md` stems matched: `sweetgrass-basket-weaving.json` and `.md`
- Every pack arrives as a draft needing human review

Brand rules and ready-signalling: see [`../README.md`](../README.md).
